package com.mycompany.a3;
/*package com.mycompany.a2;

import java.util.Iterator;

public class SpaceGame {
	private SpaceCollection theSpaceCollection;
	public SpaceGame() {
		//create the collection
		theSpaceCollection = new SpaceCollection();
		
		//add objects to the collection
		theSpaceCollection.add(new SpaceCollection("Obj1"));
		theSpaceCollection.add(new SpaceCollection("Obj2"));	
	}
	
	//Display the objects in the Collection
	public void displayCollection() {
		Iterator<E> theElement = theSpaceCollection.getIterator();
		while(theElement.hasNext()) {
			spaceObject spo = (SpaceObject) theElements.getNext();
			System.out.println(spo);
		}
			
		}
	}
	
	
}
*/